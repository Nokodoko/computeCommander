{
  "name": "websocket-server",
  "version": "1.0.0",
  "description": "WebSocket server for computeCommander k8s cluster",
  "type": "module",
  "scripts": {
    "start": "bun run src/server.ts",
    "dev": "bun --watch src/server.ts",
    "test": "bun test"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/bun": "latest"
  }
}
