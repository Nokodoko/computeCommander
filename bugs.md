1. zellij panes-frames (
   1. oem color
   2. fp not updating with the active session (session in focus in the the zellij layout)
      )
2. lazygit is not integrated
3. include ai_floater directly into the zellij layout, so I do not have to open a new terminal to invoke it, and then get another terminal window in the zellij layout, only to remove the spawning terminal session. **ORRR create a swallow function that will close the spawning terminal session, and open the ai_floater in the same session.**
