1. color output in logs, events, mail, and merge queues per agent (agent in agents pane will have matching color --
gold color for complete)
2. not just a swarm tool. agentic ide with agentic tools. (individual agent use as well)
3. all current agent activity in one pane? will need to refactor local db (per directory) to a system wide one. (major refactor, but also major shift in usage.)
4. update notification to be clickable and open the zellij tab that spawned the (specifically for intent-gate/ eval failures.)
5. need a better (and dynamic) way to gather tab numbers (for navication) and tab names for identification (need to add to intent notifications the session name, or semantic search 'session agenda' to the notification of what triggered intent)
6. add a 'swallow' function so the terminal that spawn the dashboard is swallowed by the dashboard process.
