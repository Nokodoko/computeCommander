[package]
name = "focus-watcher"
version = "0.1.0"
edition = "2021"
description = "Fast /proc-based focus watcher for cmdr dynamic panes"

[dependencies]

[profile.release]
opt-level = 2
lto = true
strip = true
